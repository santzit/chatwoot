# =============================================================================
# .env.example  –  Shared infrastructure secrets
#
# Copy this file to .env and fill in every value before running
# docker compose up.
#
# These variables are consumed by:
#   - The PostgreSQL container (POSTGRES_*)
#   - The Redis container       (REDIS_PASSWORD)
#   - The per-instance env files reference the same credentials.
# =============================================================================

# ---------------------------------------------------------------------------
# PostgreSQL  (shared cluster)
# ---------------------------------------------------------------------------
# Superuser name for the PostgreSQL cluster.
POSTGRES_USERNAME=chatwoot

# Strong password – generate with: openssl rand -hex 32
POSTGRES_PASSWORD=CHANGE_ME_STRONG_POSTGRES_PASSWORD

# ---------------------------------------------------------------------------
# Redis  (shared instance)
# ---------------------------------------------------------------------------
# Strong password – generate with: openssl rand -hex 32
REDIS_PASSWORD=CHANGE_ME_STRONG_REDIS_PASSWORD
